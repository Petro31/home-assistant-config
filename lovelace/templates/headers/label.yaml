# lovelace_gen

# card
type: markdown
card_mod:
  style: |
      ha-card {
        box-shadow: none;
        background: none;
        padding: 0px 11px;
        color: var(--header-color);
        border-width: 0px;
        font-size: 20px;
      }
content: '{{ content }}'