# lovelace_gen

type: custom:config-template-card
variables:
- states['{{ entity }}'].attributes.who
entities:
- {{ entity }}
card:
  type: custom:hui-sensor-entity-row
  icon: {{ icon }}
  name: "${ vars[0] }"
  entity: {{ entity }}
